"use strict";

import {Calculator} from "./calculator.js";

let compute1 = new Calculator(2)
console.log(compute1.sum(3).div(2).sub(1).power(8).squareRoot().display());
// execute function on calculator.js in here
